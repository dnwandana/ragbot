/** Ant Design Vue brand theme tokens — shared by the ConfigProvider in App.vue
 *  (component tree) and ConfigProvider.config in main.js (static message/Modal/
 *  notification APIs). */
export const antTheme = {
  token: {
    colorPrimary: "#FF6B35",
    colorPrimaryHover: "#E25323",
    colorBgContainer: "#ffffff",
    colorBgLayout: "#FAFAF7",
    borderRadius: 6,
    fontFamily: "'Geist', 'Inter', -apple-system, BlinkMacSystemFont, sans-serif",
    fontSize: 13,
  },
}
